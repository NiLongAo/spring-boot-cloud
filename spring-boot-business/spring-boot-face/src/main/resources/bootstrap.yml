spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
  main:
    allow-bean-definition-overriding: true
  application:
    name: face-server
  profiles:
    active: dev